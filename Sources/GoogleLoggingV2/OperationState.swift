// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

import Foundation

/// List of different operation states.
/// High level state of the operation. This is used to report the job's
/// current state to the user. Once a long running operation is created,
/// the current state of the operation can be queried even before the
/// operation is finished and the final result is available.
public enum OperationState: Codable, Equatable, Sendable {
  /// Should not be used.
  case unspecified
  /// The operation is scheduled.
  case scheduled
  /// Waiting for necessary permissions.
  case waitingForPermissions
  /// The operation is running.
  case running
  /// The operation was completed successfully.
  case succeeded
  /// The operation failed.
  case failed
  /// The operation was cancelled by the user.
  case cancelled
  /// Encodes an unknown integer value.
  ///
  /// The most common cause for an unknown values is for the service to send
  /// a value unknown to the library. We recommend you update your library to
  /// the latest version.
  case unknownIntValue(Int)
  /// Encodes an unknown string value.
  ///
  /// The most common cause for an unknown values is for the service to send
  /// a value unknown to the library. We recommend you update your library to
  /// the latest version.
  case unknownStringValue(String)

  public init() {
    self = .unspecified
  }

  /// Returns the integer value associated with the enumeration.
  ///
  /// If the enumeration was initialized with an unknown string value, this returns `nil`.
  public var intValue: Int? {
    switch self {
    case .unspecified: return 0
    case .scheduled: return 1
    case .waitingForPermissions: return 2
    case .running: return 3
    case .succeeded: return 4
    case .failed: return 5
    case .cancelled: return 6
    case .unknownIntValue(let v): return v
    case .unknownStringValue: return nil
    }
  }

  /// Returns the string value (or name) associated with the enumeration.
  ///
  /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
  public var stringValue: Swift.String? {
    switch self {
    case .unspecified: return "OPERATION_STATE_UNSPECIFIED"
    case .scheduled: return "OPERATION_STATE_SCHEDULED"
    case .waitingForPermissions: return "OPERATION_STATE_WAITING_FOR_PERMISSIONS"
    case .running: return "OPERATION_STATE_RUNNING"
    case .succeeded: return "OPERATION_STATE_SUCCEEDED"
    case .failed: return "OPERATION_STATE_FAILED"
    case .cancelled: return "OPERATION_STATE_CANCELLED"
    case .unknownIntValue: return nil
    case .unknownStringValue(let v): return v
    }
  }

  /// Initialize from a string value.
  ///
  /// If the value is unknown, this initializes to ``.unknownStringValue(_:)``.
  public init(stringValue: Swift.String) {
    switch stringValue {
    case "OPERATION_STATE_UNSPECIFIED": self = .unspecified
    case "OPERATION_STATE_SCHEDULED": self = .scheduled
    case "OPERATION_STATE_WAITING_FOR_PERMISSIONS": self = .waitingForPermissions
    case "OPERATION_STATE_RUNNING": self = .running
    case "OPERATION_STATE_SUCCEEDED": self = .succeeded
    case "OPERATION_STATE_FAILED": self = .failed
    case "OPERATION_STATE_CANCELLED": self = .cancelled
    default: self = .unknownStringValue(stringValue)
    }
  }

  /// Initialize from an integer value.
  ///
  /// If the value is unknown, this initializes to ``.unknownIntValue(_:)``.
  public init(intValue: Int) {
    switch intValue {
    case 0: self = .unspecified
    case 1: self = .scheduled
    case 2: self = .waitingForPermissions
    case 3: self = .running
    case 4: self = .succeeded
    case 5: self = .failed
    case 6: self = .cancelled
    default: self = .unknownIntValue(intValue)
    }
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.singleValueContainer()
    if let v = try? container.decode(Int.self) {
      self.init(intValue: v)
      return
    }
    if let s = try? container.decode(String.self) {
      if let v = Int(s) {
        self.init(intValue: v)
      } else {
        self.init(stringValue: s)
      }
      return
    }
    throw DecodingError.dataCorruptedError(
      in: container, debugDescription: "Expected enum value, must be integer or string.")
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.singleValueContainer()
    switch self {
    case .unspecified: return try container.encode(0)
    case .scheduled: return try container.encode(1)
    case .waitingForPermissions: return try container.encode(2)
    case .running: return try container.encode(3)
    case .succeeded: return try container.encode(4)
    case .failed: return try container.encode(5)
    case .cancelled: return try container.encode(6)
    case .unknownIntValue(let v): return try container.encode(v)
    case .unknownStringValue(let v): return try container.encode(v)
    }
  }
}
